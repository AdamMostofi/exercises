/**
 * ex002 - CLI Arguments
 *
 * Task: Print the CLI arguments passed to this script.
 *       Use process.argv to access them.
 *
 * Run with:
 *   node nodejs/level-00/ex002-cli-args.js hello 42
 *
 * The first two elements are always the node path and script path.
 * You just need to print process.argv as-is.
 *
 * EXPECTED OUTPUT (when run with: node ex002-cli-args.js hello 42):
 * [
 *   '/usr/bin/node',
 *   '/path/to/ex002-cli-args.js',
 *   'hello',
 *   '42'
 * ]
 */

// Your code below:
